version: '3.9'

services:
  spark-master:
    image: bitnami/spark:latest
    container_name: spark-master
    environment:
      - SPARK_MODE=master
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
    ports:
      - "7077:7077"       # Spark master port
      - "8080:8080"       # Web UI

    volumes:
      - /Users/serdar/PycharmProjects/huawei-data-analysis/data:/data
    networks:
      - spark-net

  spark-worker:
    image: bitnami/spark:latest
    container_name: spark-worker
    depends_on:
      - spark-master
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
    volumes:
      - /Users/serdar/PycharmProjects/huawei-data-analysis/data:/data
    networks:
      - spark-net

networks:
  spark-net:
    driver: bridge
